{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmkube.fullname" . }}-controller-manager-metrics-service
  namespace: {{ include "llmkube.namespace" . }}
  labels:
    {{- include "llmkube.labels" . | nindent 4 }}
  {{- with .Values.metrics.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.metrics.service.type }}
  ports:
  - name: {{ if .Values.metrics.secure }}https{{ else }}http{{ end }}
    port: {{ .Values.metrics.service.port }}
    protocol: TCP
    targetPort: metrics
    {{- if and (eq .Values.metrics.service.type "NodePort") .Values.metrics.service.nodePort }}
    nodePort: {{ .Values.metrics.service.nodePort }}
    {{- end }}
  selector:
    {{- include "llmkube.selectorLabels" . | nindent 4 }}
{{- end }}
